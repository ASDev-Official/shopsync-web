{
  "buildCommand": "npm run build:site",
  "outputDirectory": "dist",
  "cleanUrls": false,
  "rewrites": [
    {
      "source": "/docs/:path(.*)",
      "destination": "/docs/:path(.*)"
    },
    {
      "source": "/:path((?!/docs|.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
